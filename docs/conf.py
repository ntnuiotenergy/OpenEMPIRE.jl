# Read the Docs requires a Sphinx configuration entry even though Documenter.jl
# generates the published HTML site.
project = "OpenEMPIRE.jl"
master_doc = "index"
source_suffix = ".rst"
exclude_patterns = ["build", "build-clean"]
